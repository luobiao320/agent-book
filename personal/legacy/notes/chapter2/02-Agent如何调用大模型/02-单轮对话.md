# 单轮对话：最简单的 API 调用

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#单轮对话最简单的-api-调用

## 核心结论

最简单的一轮调用通常是：Harness 发送 `system + user`，模型返回一条 `assistant`。

重要点是：模型 API 本身是无状态的。下一轮如果还希望模型“记得”前面的内容，客户端必须把需要保留的消息重新放进请求。

## 工程理解

所谓“会话记忆”首先是 Agent 框架维护的消息历史，不是模型服务在后台自动保存的一段隐形记忆。
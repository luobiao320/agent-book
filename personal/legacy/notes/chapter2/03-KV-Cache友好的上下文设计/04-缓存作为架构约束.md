# 缓存作为架构约束

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#缓存作为架构约束

## 核心结论

当 Prompt Cache 的延迟和费用收益足够大时，缓存策略会反过来影响 Agent 架构，而不再只是事后性能优化。

典型设计包括：

- 把跨用户稳定的内容放在缓存边界之前。
- 动态的运行环境、用户信息放到后部。
- 需要继承父 Agent 前缀的子 Agent 尽量保持字节级一致。
- 工具输出被替换为摘要后，尽量固定替换文本，避免后续反复变化。

设计 Agent 时，应尽早把缓存经济性纳入上下文结构。
# Agent 状态栏在上下文中的具体位置

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#agent-状态栏在上下文中的具体位置

## 核心结论

动态状态不应每轮去修改开头的 System Prompt，而应靠近轨迹末尾追加，从而避免破坏大段稳定前缀。

一些 Harness 会借用 `user` 消息槽位注入框架生成的状态，并用专门标签标识内容来源。这里的 `user` 是协议实现选择，不等于状态真的来自终端用户。

位置靠后还使状态更接近下一次生成，通常更容易被模型关注。
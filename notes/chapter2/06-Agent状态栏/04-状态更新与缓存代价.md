# 状态更新的两种实现与缓存代价

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#状态更新的两种实现与缓存代价

## 两种方案

### 每轮替换
删除旧状态、在末尾写入最新状态。优点是只保留一份最新状态；缺点是旧状态位置之后的新后缀需要重算。

### 持久追加
旧状态不删，每轮追加一份新状态。优点是完全追加式、缓存友好；缺点是陈旧状态不断占用 token，模型必须识别最新版本。

## 实践

状态尽量由代码确定性维护。模型往往会高度信任状态栏，因此错误的计数或被污染的摘要会成为高风险输入。
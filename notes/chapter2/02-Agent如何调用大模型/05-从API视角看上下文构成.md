# 从 API 视角看上下文的构成

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#从-api-视角看上下文的构成

## 核心结构

可以把一次请求看成两部分：

- 静态前缀：System Prompt、核心工具定义。
- 动态轨迹：用户消息、模型回复、工具执行结果，以及后续注入的动态状态。

前缀尽量稳定，轨迹只增不改，是后续 KV Cache 优化的基础。

当上下文接近预算时，可以压缩较老的证据和工具输出，但应优先保留决策、约束、失败路径和来源。
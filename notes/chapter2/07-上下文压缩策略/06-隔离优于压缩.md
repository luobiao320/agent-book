# 隔离优于压缩：子 Agent 上下文隔离

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#隔离优于压缩子-agent-上下文隔离

## 核心结论

比“事后压缩大量中间信息”更彻底的方法，是让这些噪声一开始就不要进入主 Agent 的上下文。

例如把大范围代码搜索交给独立子 Agent：子 Agent 在自己的上下文里读取大量文件，主 Agent 只接收任务描述和最终结论。

## 代价

子 Agent 看不到主 Agent 的完整上下文，因此委派指令必须自包含、目标明确、约束充分。
# 压缩与 KV Cache：看似矛盾，实则互补

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#压缩与-kv-cache看似矛盾实则互补

## 核心结论

压缩通常发生在两次模型调用之间，由 Harness 重写较老的历史内容。被替换位置之后的缓存会失效，但这是一种有意识的权衡。

实践上：

- System Prompt 和稳定工具定义尽量不动。
- 优先压缩大体积、低价值的旧工具结果。
- 不要每轮都压缩，接近阈值时批量做通常更合理。
- 压缩时保留关键决策、约束、失败路径和引用来源。
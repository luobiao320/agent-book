# 为什么需要压缩：不只是长度问题

原文：https://bojieli.github.io/ai-agent-book/book/chapter2/#为什么需要压缩不只是长度问题

## 三个动机

1. 控制上下文窗口、API 成本和推理延迟。
2. 把分散原始记录提炼成更便于模型直接使用的结构化知识。
3. 避免模型在感知上下文接近上限时过早收尾等“上下文焦虑”现象。

因此，即使还没触碰窗口上限，过长且低密度的上下文也可能值得整理。